apiVersion: v1
kind: Service
metadata:
  name: devops-capstone-project
spec:
  selector:
    app: devops-capstone-project
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: NodePort
